# レッスン1: LLMの基本的なチャット機能

## 🎯 学習目標
- LLMとの基本的な対話方法を理解する
- SystemMessageとHumanMessageの役割を学ぶ
- temperatureパラメータの効果を理解する

## 📝 タスク1: ファイルを作成する

まず、`01_basic_chat.py` というファイルを作成してください。

## 📝 タスク2: 必要なライブラリをインポートする

以下のライブラリをインポートしてください：

```python
from langchain_ollama import ChatOllama
from langchain_core.messages import HumanMessage, SystemMessage
```

**解説:**
- `ChatOllama`: Ollamaのモデルを使うためのクラス
- `HumanMessage`: ユーザーからのメッセージを表すクラス
- `SystemMessage`: システム（AIの役割設定）のメッセージを表すクラス

## 📝 タスク3: モデルを初期化する

次のコードを書いてください：

```python
llm = ChatOllama(
    model="llama3.2:1b",
    temperature=0.7
)
```

**解説:**
- `model`: 使用するモデルの名前
- `temperature`: 0.0（決定的）〜 1.0（創造的）の範囲で応答のランダム性を制御

## 📝 タスク4: メッセージを作成する

SystemMessageとHumanMessageを作成してください：

```python
messages = [
    SystemMessage(content="You are a helpful AI assistant. Answer concisely in Japanese."),
    HumanMessage(content="LangChainとは何ですか？簡潔に説明してください。")
]
```

**解説:**
- `SystemMessage`: AIの性格や役割を定義します
- `HumanMessage`: ユーザーの質問を含みます
- この順番でリストに入れるのが重要です

## 📝 タスク5: LLMに問い合わせる

メッセージをLLMに送信して、応答を受け取りましょう：

```python
response = llm.invoke(messages)
print(response.content)
```

**解説:**
- `invoke()`: メッセージを送信して応答を受け取る
- `response.content`: 応答のテキスト部分を取得

## 📝 タスク6: 実行してみる

ターミナルで以下のコマンドを実行してください：

```bash
python 01_basic_chat.py
```

## ✅ 確認ポイント

- [ ] ファイルが作成できましたか？
- [ ] すべてのインポートがエラーなく動作しましたか？
- [ ] LLMから日本語で応答が返ってきましたか？

## 🔬 実験: temperatureを変えてみる

以下のコードを追加して、temperatureの違いを観察してください：

```python
print("\n=== temperature = 0.0 (決定的) ===")
llm_low = ChatOllama(model="llama3.2:1b", temperature=0.0)
response_low = llm_low.invoke([HumanMessage(content="こんにちは")])
print(response_low.content)

print("\n=== temperature = 1.0 (創造的) ===")
llm_high = ChatOllama(model="llama3.2:1b", temperature=1.0)
response_high = llm_high.invoke([HumanMessage(content="こんにちは")])
print(response_high.content)
```

同じ質問で何度か実行して、応答の違いを確認してください。

## 💡 理解度チェック

以下の質問に答えられますか？

1. SystemMessageとHumanMessageの違いは何ですか？
2. temperatureパラメータは何を制御しますか？
3. `invoke()`メソッドは何を返しますか？

## 🎉 完了したら

完了したら、`LESSON_02.md` を開いてレッスン2に進んでください！
